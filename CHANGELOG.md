# 更新日志

本文件记录项目的重要版本变更。

## v0.4.0 - 2026-08-30

### 🎯 核心主题

体验质变 + 工程成熟：AI 回复全面流式化，题库隐藏用例自动判题，基于评分数据的薄弱方向分析，并引入数据库迁移、配置集中管理、容器化与 CI。

### ✨ 新功能

**流式输出（SSE）**
- `/interview/review`、`/interview/answer`、`/interview/evaluate` 改为 Server-Sent Events 流式响应
- `chat_stream()` 增量生成器，前端 `streamPost()` 逐字渲染，AI 回复"打字机"效果
- 流结束后才执行会话摘要刷新、总分提取等收尾逻辑

**自动判题**
- `POST /interview/verify`：用题库隐藏用例跑用户代码，返回通过率与逐用例结果
- `/interview/review` 自动判题并把「通过 X/Y 用例」注入点评上下文
- 前端「✅ 判题」按钮 + 通过率徽章 + 逐用例卡片；LLM 生成题优雅降级

**薄弱方向分析**
- 会话索引新增评分字段，面试加权总分自动回写
- `GET /auth/weaknesses`：按方向聚合均分升序排列；样本不足 3 场不给结论
- 个人中心展示薄弱方向 + 一键「去练习」直达对应方向面试

### 🛠 工程化

**数据库迁移（Alembic）**
- 完整初始迁移（5 张表 + 索引），空库验证可执行，现网库已 stamp 基线
- 以后模型变更必须带迁移脚本，告别手动 ALTER TABLE

**配置集中管理（pydantic-settings）**
- `config.py` 统一管理数据库/Redis/Judge0/JWT/CORS/AI 全部配置
- CORS 允许源可配置化（JSON 数组）

**容器化一键启动**
- 后端/前端 Dockerfile + .dockerignore
- 根 `docker-compose.yml`（include Judge0 全家桶）：一条命令启动全部服务
- PostgreSQL 首启自动创建知识库用户与数据库（kb-init.sql）

**CI（GitHub Actions）**
- push/PR 自动运行：后端 pytest（PG + Redis 服务容器）+ 前端 lint/build

### 🔒 安全

- 登录限流：同用户名连续失败 5 次锁定 15 分钟，成功后清零（Redis 计数）

### 📄 文档

- 根 README 重写：架构图（mermaid）、功能清单、Docker 与本地两种启动方式、环境变量表、测试与迁移说明

### 🧪 测试

- 后端测试扩展至 107 项，新增：判题逻辑与端点、评分聚合与短板排序、登录限流（fake Redis 语义测试）

---

## v0.3.0 - 2026-08-30

### 🎯 核心主题

新增完整的**用户认证体系**，让面试历史、题目评价从"绑定设备"升级为"跟随账号"，同时修复了原有接口信任客户端 `user_id` 的越权隐患。

### ✨ 新功能

**用户认证（JWT + bcrypt）**
- `users` 表：字符串 UUID 主键（与现有 user_id 体系兼容）、bcrypt 密码哈希
- 注册/登录/登出/当前用户接口（`/auth/*`），用户名 + 密码方式
- JWT 含唯一 `jti`，登出通过 Redis 黑名单实现真实失效
- 输入校验：用户名 3-20 位字母数字下划线，密码至少 8 位

**会话索引持久化（PostgreSQL）**
- `user_sessions` 表：会话标题/方向/难度/时间的轻量索引，永久保存
- 会话消息仍存 Redis（热数据 + TTL），形成 PG 存真相、Redis 存热数据的分层
- 会话列表改从 PG 索引读取

**先玩后登录：匿名历史合并**
- 匿名用户可无账号使用全部功能
- 注册/登录时可携带匿名 ID，后端自动迁移：PG 会话索引与题目评价 + Redis 会话 zset 与已用题集合
- 前端登录后身份无缝切换为账号 ID

**接口鉴权（封越权漏洞）**
- 登录用户身份一律以 token 为准，忽略客户端传入的 user_id
- 会话加载：登录用户只能访问自己的会话（403）
- 会话删除：token 身份 + 同步删除 PG 索引

**个人中心**
- `GET /auth/stats`：面试场次、方向/难度分布、题目评价数
- `POST /auth/password`：修改密码，成功后自动拉黑当前 token 强制重新登录
- 前端个人中心弹窗：统计卡片 + 分布标签 + 改密表单

**前端认证体验**
- 登录/注册弹窗（模式切换、错误提示、携带匿名 ID）
- axios 拦截器：自动附加 Bearer 头、token 失效自动清理登录态并提示
- 顶栏登录态：用户名（可点击进个人中心）+ 登出

### 🐛 修复

- 修复接口信任客户端 user_id 的越权风险
- 修复前端登录后 `userId` 状态不更新的隐患（统一改为实时读取身份）
- 修复 `user_sessions.user_id` 列宽装不下匿名 ID（`user-` 前缀 41 字符）的问题
- 清理 docker-compose 中废弃的 `kb-db` 服务（postgres:16 镜像拉取卡顿的根源）

### 🧪 测试

- 后端测试扩展至 91 项，新增：密码哈希与校验、注册去重、登录成败、JWT 往返、鉴权依赖（含黑名单）、改密四分支、会话索引 CRUD、匿名数据迁移、个人统计聚合
- HTTP 层端到端验证：注册 → 鉴权访问 → 匿名模式 → 登出失效 → 改密后旧 token 失效

---

## v0.2.0 - 2026-08-25

### 🎯 核心主题

本版本在初版「AI 面试模拟」的基础上，完成两大方向的建设：

1. **LLM 成本与上下文工程化**：解决"全量历史 + 高频调用 + 长上下文"带来的 token 成本膨胀问题。
2. **题库知识库系统**：将题目和评分标准从"每次由 LLM 凭空生成"升级为"结构化存储 + 检索复用 + 质量闭环"，显著提升出题质量与系统可演进性。

### ✨ 新功能

**题库知识库（PostgreSQL）**
- 新增 `questions`、`question_ratings`、`rubrics` 三张表，使用 SQLAlchemy 2.x 异步 ORM
- 题目支持按难度、语言、方向分类，附标签、示例用例、隐藏测试用例与参考解法
- 题目生命周期状态机：`draft → new/failed → published/rejected`，含非法流转拦截

**三层质检流水线（题目质量把关）**
- 第一层：规则结构校验（确定性）
- 第二层：Judge0 实跑参考解法并逐条比对测试用例（确定性，绕开 LLM 同源偏差）
- 第三层：LLM 审题（语义辅助）
- `batch_generate.py` 一键批量生成 + 质检 + 入库，生成题以 `new` 状态进入用户验证池

**检索式出题**
- `/interview/start` 题库优先：命中直接返回题目，零 LLM 调用；未命中降级为 LLM 生成
- 选题策略：published 优先 → 低频优先 → 随机，Redis 记录用户已用题避免重复出题

**题目评价与自动裁决**
- 面试页支持对题目点赞/踩，评价记录持久化
- 冷启动 10 条评价后自动裁决：好评率 ≥ 70% 转正，差评率 ≥ 50% 踢出

**评分标准双视图**
- 同一套底层评分维度生成两份视图：考官版（注入 LLM 评分 prompt）与成长版（面向用户的正向说明）
- 评分标准按方向/难度检索，逐级回退到通用标准
- `/interview/evaluate` 自动从评分报告提取加权总分回写题目均分

**题库管理页（前端）**
- 新增「题库管理」视图：状态/难度/方向筛选、审核流转、删除
- 题库健康度看板：总题数、活跃题、好评率、待校准难度等
- 题目卡片展示均分与难度校准建议

### ⚡ LLM 成本优化（七步）

1. 会话摘要字段：为长期记忆压缩铺路
2. 统一上下文构造器：只保留系统提示 + 摘要 + 近期消息
3. 自动生成会话摘要：压缩长期记忆
4. 历史裁剪：限制消息数量与单条长度，防止长代码撑爆 prompt
5. 执行结果摘要：Judge0 输出压缩为关键错误与状态，不再全量粘贴
6. 重复反馈缓存：相同代码 + 相同状态直接复用点评，避免重复推理
7. Token 预算与兜底：调用前裁剪超长 prompt，模型异常时返回安全降级文案

### 🐛 修复

- **Judge0 沙箱在 cgroup v2 主机（Windows Docker Desktop / WSL2）上全部 `Internal Error`**：通过开启 per-process 限制参数禁用 isolate 的 cgroup 标志，并将共享 `/box` 卷改为可写，修复后代码执行恢复正常
- 修复 `init_db` 未注册模型导致建表空转的问题
- 修复 PostgreSQL 下时间列时区不一致导致写入报错的问题（列类型统一为 timezone-aware）
- 修复质检通过的生成题入库状态错误（draft → new），并增加同标题去重
- 修复旧版端点测试与新增 Redis 依赖不兼容的问题

### 🧪 测试

- 后端测试套件扩展至 63 项，覆盖：摘要与裁剪、质检管线、知识库 CRUD、状态机、自动裁决、评分标准检索、难度校准、看板统计
- 测试覆盖确定性逻辑（状态机、比较器、校验规则），外部依赖（Judge0、LLM）通过 fake seam 隔离

### 📦 基础设施

- 知识库复用 Judge0 自带 PostgreSQL 实例（独立数据库 `interview_kb` + 低权限用户），端口 `5433`
- 种子数据：手工黄金题（published）+ 双视图评分标准，幂等导入

---

## v0.1.0 - 初版

- AI 面试模拟器：FastAPI + React + DeepSeek + Judge0
- 支持选题方向/难度/语言，AI 出题、代码执行、AI 点评与最终评分
- 多会话管理与 Redis 会话存储
